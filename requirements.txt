# Core
torch>=2.2
torchvision>=0.17
numpy>=1.24
einops>=0.7            # clean attention/reshape ops (token <-> grid)
pyyaml>=6.0            # config loading
tqdm>=4.66

# Encoders / backbones
transformers>=4.56     # DINOv3 ViT (DINOv3ViTModel); the frozen encoder loads through this
# NOTE: DINOv3 weights are gated and not on PyPI. Download the HF checkpoint
#       (facebook/dinov3-vitl16-pretrain-lvd1689m), put the local file path in
#       configs/model.yaml -> encoder.checkpoint. The HF-format state dict loads directly.

# Training / logging
tensorboard>=2.16
# wandb>=0.17         # optional, uncomment if you prefer Weights & Biases

# Metrics / geometry
scipy>=1.11           # rotation helpers, eval metrics

# Image / data IO  (adjust to your sim data format)
pillow>=10.0
opencv-python>=4.9
# h5py>=3.10          # uncomment if sim data ships as HDF5

# Dev / tests
pytest>=8.0
